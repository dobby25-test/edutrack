# Frontend Source

`frontend/src/` has all frontend code.

## Folders
- `components/` -> UI pages and modules
- `services/` -> API calls
- `hooks/` -> reusable hooks
- `utils/` -> helpers

## Working flow
1. routes render components
2. components call services
3. services call backend
4. response updates component state
